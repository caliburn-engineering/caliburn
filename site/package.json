{
  "name": "caliburn-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "./sync-demo.sh && astro build && wrangler pages deploy dist --project-name caliburn-engineering --branch main",
    "sync:demo": "./sync-demo.sh"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
